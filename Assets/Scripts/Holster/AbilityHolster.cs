using UnityEngine;

public class AbilityHolster : MonoBehaviour
{
    public RingMenu menu;
    void Update()
    {
        // // Example: toggle on input
        // if (Input.GetKeyDown(KeyCode.Tab))
        //     menu.gameObject.SetActive(!menu.gameObject.activeSelf);
    }
}
